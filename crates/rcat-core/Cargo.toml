[package]
name = "rcat-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Core domain types, file detection, and Viewer trait for rcat"

[dependencies]
memmap2 = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
infer = "0.19"   # Used for first-pass magic byte detection by the core

[dev-dependencies]
tempfile = "3"
