# rust-cat Design & Implementation Plan

**This is a snapshot / reference copy.**

The **authoritative, living version** of the full high-level design, architecture, Viewer trait details, extensibility strategy, phased roadmap, keyboard spec, CLI surface, verification plan, and GitHub DevEx recommendations lives in the active development session document.

During active development the session plan.md is the single source of truth.

## Quick Links (from the approved plan)

- Program definition & scope (MVP v0.1 vs future)
- High-level architecture (Viewer trait + unified byte offset model)
- Recommended workspace crate layout
- Keyboard navigation table
- Proposed CLI flags
- Implementation phases (0–5)
- Verification & quality strategy
- Git + GitHub hosting requirements (Section 13)
- Progress log (most recent at bottom of the full plan)

If you need the complete current plan, ask the maintainer for the latest session export or check the `.grok/sessions/.../plan.md` file in the development environment.

## Current Status (as of last update)

See the Progress Log section in the full plan.

Phase 0 (skeleton + working `rcat --version` + first commit) is complete.

We are entering Phase 1 (core foundations).
